# 🏭 Siemens Teamcenter 2506 폴더 구조 가이드

> 설치 폴더 분석, 역할, 커스터마이징, 백업 가이드  
> 📅 분석일: 2025년 12월 | 📍 경로: `C:\Siemens`

---

## 📊 전체 폴더 구조 개요

```
C:\Siemens\
├── 📂 Teamcenter2506/      (273,135 files) - 📌 핵심 TC 시스템
├── 📂 DeploymentCenter/    (79,544 files)  - 배포 관리 도구
├── 📂 NX2506/              (105,252 files) - NX CAD 통합
├── 📂 Designcenter2506/    (7,683 files)   - 설계센터
├── 📂 apache-tomcat-9.0.98/ (770 files)    - 웹 서버
├── 📂 License Server/      (43 files)      - 라이선스 관리
└── 📂 Installation_file/                   - 설치 파일
```

---

## 🔧 Teamcenter2506 핵심 구조

### 📂 teamcenter_root (116,722 files)
TC 서버 실행 환경의 핵심 폴더

| 폴더 | 파일수 | 역할 |
|------|--------|------|
| **bin/** | 1,671 | 실행파일, 유틸리티, DLL 라이브러리 |
| **aws2/** | 26,207 | Active Workspace 2 웹 클라이언트 |
| **portal/** | 3,712 | Rich Application Client (RAC) |
| **bmide/** | 3,205 | Business Modeler IDE - 데이터모델 커스터마이징 |
| **include/, include_cpp/** | 6,962 | C/C++ 개발 헤더 파일 |
| **install/** | 11,128 | 설치/업그레이드 스크립트 |
| **logs/** | 1,676 | 서버 로그 파일 (정리 가능) |
| **microservices/** | 18,711 | 마이크로서비스 컴포넌트 |
| **solr-9.6.0/** | 2,313 | 전문 검색 엔진 (Full Text Search) |
| **lib/** | 435 | 라이브러리 파일 |
| **lang/** | 5,966 | 다국어 지원 파일 |
| **filerepo/** | 20,549 | 파일 레포지토리 |

### 📂 tc_data (150,380 files)
설정, 데이터, 템플릿 파일

- **tc_preferences.xml** - 환경 설정
- **clientcache/** - 클라이언트 캐시 (145,460 파일)
- **model/** - 데이터 모델 정의 (1,062 파일)
- **tc_profilevars.bat** - 환경변수 스크립트
- **logger.properties** - 로깅 설정
- NX CAM 템플릿 (.prt 파일들)

### 📂 volumes (5,518 files)
파일 저장소 (Vault)

- **DefaultVolume/** - 실제 파일 저장소
- CAD 파일, 문서 등 저장
- ⚠️ **반드시 백업 필요!**

### 📂 Dispatcher (515 files)
배치/백그라운드 작업 처리

- DispatcherClient/ - 클라이언트
- Scheduler/ - 스케줄러
- Module/ - 변환 모듈
- 번역, 변환 작업 처리

---

## 🛠️ 커스터마이징 필수 경로

### 데이터 모델 커스터마이징 (BMIDE)

```
teamcenter_root\bmide\
├── templates/   - 템플릿 프로젝트
├── workspace/   - BMIDE 작업공간
└── client/      - BMIDE 클라이언트
```

**주요 작업:**
- 프로퍼티, 타입, 룰 정의
- LOV, 객체 관계 설정
- 워크플로우 템플릿 생성

### 서버 확장 개발 (ITK)

| 경로 | 용도 |
|------|------|
| `teamcenter_root\include\` | C API 헤더 |
| `teamcenter_root\include_cpp\` | C++ 헤더 |
| `teamcenter_root\lib\` | 링크 라이브러리 |

**주요 작업:**
- User Exits, Custom Handlers
- ITK 프로그래밍

### 클라이언트 커스터마이징

| 경로 | 용도 |
|------|------|
| `teamcenter_root\portal\plugins\` | RAC 플러그인 |
| `teamcenter_root\portal\registry\` | UI 설정 |
| `teamcenter_root\aws2\` | AWC 커스터마이징 |

### 환경 설정 파일

| 파일 | 경로 | 용도 |
|------|------|------|
| **tc_preferences.xml** | tc_data\ | 시스템 환경설정 |
| **tc_profilevars.bat** | tc_data\ | 환경변수 |
| **tc_setup.bat** | tc_data\ | 초기 설정 |
| **logger.properties** | tc_data\ | 로깅 설정 |

---

## 📝 주요 개발 소스/설정 파일

| 파일/폴더 | 위치 | 용도 |
|-----------|------|------|
| **tc_preferences.xml** | tc_data\ | 시스템 전체 환경설정 (Preferences) |
| **tc_profilevars.bat** | tc_data\ | 환경변수 설정 스크립트 |
| **process_templates_base.dat** | tc_data\ | 워크플로우 템플릿 정의 |
| **datamapping.xml** | tc_data\ | CAD 데이터 매핑 규칙 |
| **defaultTcxmlScopeRules.xml** | tc_data\ | TCXML 내보내기 범위 규칙 |
| **tc_am_rule_tree.xml** | tc_data\ | 접근관리(AM) 룰 트리 |
| **bmide\templates\** | teamcenter_root\ | BMIDE 데이터모델 템플릿 |
| **nxmanager_exports.dat** | tc_data\ | NX 통합 내보내기 설정 |
| **model\** | tc_data\ | 데이터 모델 정의 파일 (1,062개) |

---

## 🗑️ 삭제 가능한 파일/폴더

### ✅ 안전하게 삭제 가능

| 폴더/파일 | 설명 |
|-----------|------|
| `teamcenter_root\logs\` | 오래된 로그 파일 |
| `tc_data\log_*\` | 데이터 로그 |
| `apache-tomcat\logs\` | Tomcat 로그 |
| `Dispatcher\Logs\` | 디스패처 로그 |
| `teamcenter_root\temp\` | 임시 파일 |
| `apache-tomcat\temp\` | Tomcat 임시 |
| `apache-tomcat\work\` | 컴파일 캐시 |
| `*.log, *_importer.log` | 로그 파일들 |
| `*_validation.out` | 검증 출력 파일 |
| `*_failed_objects.xml` | 실패 로그 |

### ⚠️ 주의해서 삭제

| 폴더 | 설명 |
|------|------|
| `tc_data\clientcache\` | 145,460 파일 - 재생성 가능, 삭제 시 재생성 필요 |
| `DeploymentCenter\repository\software\` | 설치 후 불필요 시 삭제 검토, 업그레이드 시 필요할 수 있음 |
| `Designcenter2506\*.zip` | 설치 완료 후 검토 |

### 🚫 절대 삭제 금지

> [!CAUTION]
> 다음 폴더는 절대 삭제하지 마세요!

- `volumes\DefaultVolume\` - 실제 사용자 데이터!
- `tc_data\pom_schema_*` - DB 스키마
- `teamcenter_root\bin\` - 실행파일

---

## 💾 백업 필수 폴더

| 폴더 | 우선순위 | 백업 빈도 | 설명 |
|------|----------|-----------|------|
| **volumes\DefaultVolume\** | 🔴 최우선 | 매일 | 모든 사용자 데이터 파일 저장소 |
| **데이터베이스** | 🔴 최우선 | 매일 | Oracle/SQL Server DB 백업 |
| **tc_data\** | 🟠 필수 | 변경 시 | 모든 설정 및 환경변수 |
| **bmide\templates\** | 🟠 필수 | 변경 시 | 커스터마이즈된 데이터모델 |
| **portal\plugins\** (커스텀) | 🟡 권장 | 변경 시 | 커스텀 플러그인 |
| **Dispatcher\Module\** | 🟡 권장 | 변경 시 | 커스텀 변환 모듈 |
| **License Server\** | 🟡 권장 | 초기 설정 후 | 라이선스 설정 |

> [!IMPORTANT]
> **백업 팁**
> - Volume과 DB는 **동시에 백업**해야 데이터 일관성 유지
> - 백업 전 **서비스 중지** 권장
> - 커스터마이징 파일은 **버전관리(Git)** 권장
> - 정기 백업 자동화 스크립트 구축 필요

---

## 📦 기타 주요 폴더 설명

### DeploymentCenter
설치/업그레이드/패치 관리 도구

```
DeploymentCenter\
├── repository/
│   ├── software/        (60,696) - 설치 소프트웨어
│   └── dc_contributions/ - 기여 패키지
└── webserver/           - DC 웹서버
```

> 💡 설치 완료 후 software 폴더 정리 검토

### NX2506 (NX 통합)
CAD 작업 시 NX와 TC 연동

| 폴더 | 역할 |
|------|------|
| UGII/ | NX 핵심 모듈 |
| UGMANAGER/ | TC 통합 모듈 |
| NXBIN/ | 실행 바이너리 |
| MACH/ | 가공 모듈 |
| SIMULATION/ | 시뮬레이션 |

### apache-tomcat-9.0.98
AWC (Active Workspace) 웹서버

| 폴더 | 역할 |
|------|------|
| webapps/ | 웹 애플리케이션 |
| conf/ | 설정 파일 |
| logs/ | 서버 로그 (삭제 가능) |

### License Server
라이선스 관리

| 파일 | 역할 |
|------|------|
| lmgrd.exe | 라이선스 데몬 |
| lmtools.exe | 관리 도구 |
| saltd.exe | Siemens 라이선스 |

---

## 🔧 주요 유틸리티 (bin 폴더)

### 서버 관리
- `tcserver.exe` - TC 서버 실행
- `install.exe` - 설치 유틸리티
- `database_verify.exe` - DB 검증
- `preferences_manager.exe` - 설정 관리
- `task_monitor.exe` - 작업 모니터링

### 데이터 관리
- `item_export.exe` - 아이템 내보내기
- `item_import.exe` - 아이템 가져오기
- `plmxml_export.exe` - PLMXML 내보내기
- `tcxml_export.exe` - TCXML 내보내기
- `data_sync.exe` - 데이터 동기화

### 사용자/보안
- `make_user.exe` - 사용자 생성
- `role_util.exe` - 역할 관리
- `am_install_tree.exe` - 접근권한 설치

### 개발/디버깅
- `bmide_*.bat` - BMIDE 관련 도구들
- `generate_client_meta_cache.exe` - 메타 캐시 생성
- `csv2tcxml.bat` - CSV to TCXML 변환

### 유지보수
- `database_walker.exe` - DB 탐색
- `purge_*.exe` - 정리 유틸리티
- `cleanup_*.exe` - 정리 도구
- `migrate_*.exe` - 마이그레이션

---

## 📋 요약 및 권장사항

### ✅ 핵심 체크리스트

- [ ] **volumes\DefaultVolume\** + DB 정기 백업
- [ ] **tc_data\** 변경 시 백업
- [ ] 커스텀 코드는 Git으로 버전관리
- [ ] 로그 폴더 정기 정리 (90일 이상)
- [ ] temp 폴더 정기 정리
- [ ] 업그레이드 전 전체 백업

### 📚 개발자 필수 경로

| 경로 | 용도 |
|------|------|
| `bmide\` | 데이터모델 개발 |
| `include\, include_cpp\` | ITK 개발 |
| `tc_data\` | 설정 커스터마이징 |
| `portal\plugins\` | RAC 플러그인 |
| `aws2\` | AWC 커스터마이징 |

### 🔄 정기 유지보수 작업

| 작업 | 빈도 | 담당 |
|------|------|------|
| Volume + DB 백업 | 매일 | 시스템 관리자 |
| 로그 정리 | 주간/월간 | 시스템 관리자 |
| 라이선스 모니터링 | 월간 | 라이선스 관리자 |
| 보안 패치 확인 | 분기 | 보안 담당자 |

---

> 📂 **파일 위치**: `C:\Siemens\Teamcenter2506_Guide.md`  
> 🌐 **HTML 버전**: `C:\Siemens\Teamcenter2506_Guide.html`
